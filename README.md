# regutoolsData

Repository with the regulonDB sqlite database.

The file regulondb_v10.8_sqlite.db is used to create a local copy of the 
regulonDB database v10.8 through regutools::connect_database().
